# Suggested GitHub profile metadata

## Profile description

Genomics Data Scientist and junior ML Engineer building interpretable ML pipelines, health AI products and reproducible analytics.

## Short bio

Genomics & Health AI | Python, ML, multilabel NLP, SQL, Streamlit | Reproducible data products

## Suggested pinned repositories

1. `EcoMeal_Bot`
2. `analyse-marche-bitcoin-correlations`
3. `olympic-sql-database`
4. `analyse-economique-environnementale-globale`

Do not pin the legacy Twitter repository as a primary project.

## Suggested repository topics

| Repository | Suggested topics |
|---|---|
| `EcoMeal_Bot` | `streamlit`, `local-llm`, `python`, `recommendation-system`, `sustainability`, `data-product` |
| `analyse-marche-bitcoin-correlations` | `python`, `etl`, `time-series`, `plotly`, `financial-data`, `correlation-analysis` |
| `olympic-sql-database` | `sql`, `sqlite`, `data-modeling`, `window-functions`, `synthetic-data`, `pytest` |
| `analyse-economique-environnementale-globale` | `python`, `data-analysis`, `data-visualization`, `etl`, `world-bank`, `our-world-in-data` |

## Manual actions after validation

1. Create the public repository `AymaneAcharki/AymaneAcharki`.
2. Push this local `main` branch only after reviewing every statement.
3. Add the profile description/bio manually if desired.
4. Create and publish `olympic-sql-database` after its tests and license review pass.
5. Apply repository topics and descriptions manually.
6. Pin the four repositories in the order above after their refreshed branches are merged.
7. Keep VoxPath and Scriptum Clinica private; mention them only at the high level used in `README.md`.
